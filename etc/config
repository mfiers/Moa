plugins: [fileset, configure, newjob, logo, test, adhoc, help, template, info, extraCommands, parameterCheck, prompt]
mancommand: "nroff -c -mandoc 2>/dev/null | less -is"
pandocbin: pandoc
use_ansi: True